import {
  standard0,
  standard1,
  standard2,
  retro1
} from "../../../resources/map/styles";

const mapStyle = {
  styles: standard0,
  minZoom: 14, // 0 is the world map
  maxZoom: 17
};

export default mapStyle;
